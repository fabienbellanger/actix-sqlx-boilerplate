concurrency: 10
base: 'http://localhost:8089'
iterations: 100
rampup: 0

plan:
  - name: Health Check
    request:
      url: /health-check
  
  - name: Login
    request:
      url: /v1/login
      method: POST
      body: '{"email": "valentil@gmail.com", "password": "0000"}'
      headers:
        Content-Type: 'application/json'
