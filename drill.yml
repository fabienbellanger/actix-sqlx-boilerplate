concurrency: 100
base: 'http://localhost:8089'
iterations: 2000
rampup: 0

plan:
  - name: Health Check
    request:
      url: /health-check
  
  - name: Login
    request:
      url: /v1/login
      method: POST
      body: '{"email": "test@gmail.com", "password": "00000000"}'
      headers:
        Content-Type: 'application/json'
