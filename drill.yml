concurrency: 10
base: 'http://localhost:8089'
iterations: 100
rampup: 0

plan:
  - name: Health Check
    request:
      url: /health-check
  - name: List of all users
    request:
      url: /v1/users
